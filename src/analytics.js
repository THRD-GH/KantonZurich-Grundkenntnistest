// Cloudflare Web Analytics — privacy-friendly, cookieless visit counting.
//
// How to enable:
//   1. Create a free account at https://dash.cloudflare.com  →  "Web Analytics".
//   2. "Add a site" and enter the hostname  thrd-gh.github.io
//      (Cloudflare scopes data by the token below, so you'll only see this app's hits.)
//   3. Copy the token from the snippet it shows (the value of data-cf-beacon's "token").
//   4. Paste it between the quotes below and commit.
//
// The token is NOT a secret — it is visible in the page source by design, so it is fine to commit.
// While this is empty, no analytics script is loaded and nothing is sent.
export const CF_BEACON_TOKEN = "";
